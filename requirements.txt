# Runtime-only dependencies for the Vercel Python serverless function
# (api/index.py). Offline/data-gen deps (xgboost, yfinance, pandas, faker,
# openai, uvicorn) are excluded to keep the function under Vercel's 500MB
# limit. scikit-learn is also excluded (it pulls in scipy, ~170MB combined):
# the RAG keyword retriever is reimplemented numpy-only in backend/rag/store.py.
# Full local install still uses backend/requirements.txt.
fastapi>=0.110
sqlalchemy>=2.0
pydantic>=2.0,<3
python-dotenv>=1.0
anthropic>=0.34
numpy>=1.26
langgraph>=0.2
truststore>=0.9
